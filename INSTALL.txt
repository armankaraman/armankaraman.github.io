Замените в корне репозитория:
  index.html
  script.js
  background-video.js

Добавьте:
  assets/previews/hero.jpg

Фон сразу использует существующий assets/hero.mp4.
Сам оригинал не изменён. hero-scroll.mp4 больше не запрашивается.
hero.jpg — маленький постер первого кадра, пока загружается видео.
Остальные файлы и папку assets не удаляйте.
Это пакет исправлений, а не полный сайт.
После публикации обновите страницу с Ctrl+F5.

Исправления:
- Удалена ссылка на отсутствующий hero-scroll.mp4 (сервер возвращал 404).
- Добавлен отсутствующий постер hero.jpg.
- Загрузка фона начинается прямо из HTML, скрипт не сбрасывает её повторным load().
- Декодирование запускается без ожидания клика; остаётся повтор при жесте.
- Ошибка видео не скрывает постер; при сетевой ошибке предусмотрен один повтор.
- Перемотка ограничена длительностью; одновременно выполняется один seek.
- Расчёт диапазона прокрутки вынесен из обработчика каждого скролла.
- Исправлена геометрия определения видимой галереи.
- Все видимые видеокарточки проигрываются; невидимые приостанавливаются.
- Статичные изображения запрашиваются сразу, без ожидания lazy loading.
- Восстановление воспроизведения после возврата на страницу и жеста.
- Для миниатюр видео без постера подключается источник.
- При ошибке облегчённого изображения используется оригинал.
- Обновлены версии скриптов в HTML для обхода старого кеша.

Проверено:
- Синтаксис всех переданных JS-файлов.
- Автоматическая проверка логики фона: холодный/готовый старт,
  перемотка до конца и обратно, открытие/закрытие модального окна,
  ограниченный повтор после сетевой ошибки, завершение цикла анимации.
- Оригинальный hero.mp4 доступен, размер 3 111 801 байт,
  1920x1080, около 10 секунд. Faststart уже есть.

Ограничения:
- Полный браузерный прогон на компьютере и телефоне не выполнен.
- Остальные медиа не были приложены; все галерейные файлы и внешние
  Sketchfab-встраивания поштучно не проверены.
- Новые файлы ещё не опубликованы в GitHub Pages.
