# Проверка галерей — 2026-09-07

Реализация проверена локально: http://127.0.0.1:4173/ . Push и публикация не выполнялись.

## Функциональные результаты

66/66 автоматических проверок прошли в установленном Microsoft Edge, headless, через Playwright Python. Проверочные сценарии: `.local-preview/check.py`, `.local-preview/touch-check.py`; результаты: `.local-preview/results.json`, `.local-preview/touch-results.json`.

- Desktop: 1440 × 1000 CSS px. Mobile: 390 × 844 CSS px, DPR 1, touch emulation. Дополнительные размеры: 320 × 740 и 844 × 390.
- Все шесть видеопроектов и четыре статичные карточки проходят вперёд и назад; конечные секции отпускают вертикальную прокрутку и снова удерживаются при возвращении.
- Проверены wheel в обе стороны, эмулированные вертикальные touch-свайпы в обе стороны в обеих галереях и все ссылки меню.
- Видимые видео проигрываются одновременно, muted/loop включены, время меняется без прокрутки; шестой ролик тоже воспроизводится. Ошибок загрузки десяти локальных файлов проектов не обнаружено.
- Мини-галереи открываются, показывают существующие дополнительные видео и изображение Taulan, позволяют вернуться к главному видео. Закрытие кнопкой и Escape возвращает точный scrollY и активную карточку. Заглушки статичных проектов тоже открываются.
- Медиа в мини-галерее используют contain; пропорции не растягиваются. Описание активной мобильной карточки видно без hover.
- Sketchfab: шесть ссылок в сетке 3 × 2, в том числе на мобильном и узком экране. Реальные миниатюры загрузились. Iframe не создаются. В данных сохранены восемь записей.
- Горизонтального переполнения страницы в проверенных размерах нет. JavaScript-исключений в desktop/mobile прогонах нет. `git diff --check` проходит.

## Визуальные материалы

Источник: приложенный пользователем составной референс 1536 × 1024 px, desktop + dialog + mobile. Отдельного исходного файла в рабочей папке нет.

Рендеры, DPR 1:

- `.local-preview/desktop-motion.png`, `.local-preview/desktop-stills.png`, `.local-preview/desktop-models.png`, `.local-preview/desktop-dialog.png` — 1440 × 1000.
- `.local-preview/mobile-motion.png`, `.local-preview/mobile-stills.png`, `.local-preview/mobile-models.png`, `.local-preview/mobile-dialog.png` — 390 × 844.

Просмотрены полные рендеры Motion, Stills, моделей и диалога; отдельное внимание уделено мобильным подписям и миниатюрам. Первая итерация выявила обратный наклон боковых карточек и слишком высокие desktop-карточки: знак поворота исправлен, высота уменьшена. Диалог получил более компактный светлый заголовок с обычным регистром. После исправлений сделаны новые снимки и повторены функциональные проверки.

Проверка пяти визуальных поверхностей:

- Типографика: сохранён существующий системный шрифт, небольшие заголовки разделов с разрядкой; заголовок диалога светлый, обычного регистра.
- Отступы и компоновка: центрированная карточка, зеркальная перспектива соседних; мобильные соседние карточки частично видны; сетка моделей строго три колонки.
- Цвета: использованы существующие тёмные токены, тонкие серые границы; дополнительные декоративные эффекты не вводились.
- Изображения: реальные локальные видео и дополнительные файлы; реальные обложки Sketchfab. Четыре Stills-заглушки явно разрешены пользователем. Кадры роликов и обложки моделей отличаются от иллюстративных кадров референса, поскольку использованы текущие материалы.
- Тексты: сохранены реальные названия, порядок и описания. Hero, About, Experience и Contact сохранены по требованию, поэтому полная страница отличается от сокращённого референса.

## Ограничения визуальной проверки

Не выполнено нормализованное сравнение референса и рендера в одном изображении при одинаковом масштабе и состоянии. Составной референс и реальные видеокадры не являются пиксельно совпадающими состояниями. Поэтому строгая проверка 1:1 не заявляется пройденной; функциональная проверка выше завершена отдельно.

Не проверены физические iOS/Android устройства, Safari/Firefox и ограничения автопроигрывания в режимах энергосбережения. Проверен именно Edge с эмуляцией touch, а не реальный телефон. Переход во внешнюю интерактивную модель Sketchfab не тестировался; проверены адреса ссылок, загрузка миниатюр и отсутствие iframe.

final result: blocked

Статус относится только к строгому визуальному сравнению 1:1; блокирующее условие — отсутствие нормализованного совместного сравнения исходного референса и реализации. Функциональные проверки: passed, 66/66.

## Обновление: вступление и плавность

Начальная 3D-сцена удалена из страницы, загрузка `hero-3d.js`, Three.js и GLB отключена. Страница начинается с Motion. Исходный файл сцены и модель оставлены на диске.

Движение обеих галерей сглажено по времени через requestAnimationFrame (85 мс), без изменения нативной прокрутки; prefers-reduced-motion отключает сглаживание. Дополнительно проверено в Edge при 1440 × 1000 и 390 × 844: отсутствие вступления и запросов WebGL-ресурсов, промежуточные положения карточек после прокрутки, движение назад, точное восстановление scrollY и карточки после закрытия, отсутствие JS-исключений. Снимки: `.local-preview/start-1440.png`, `.local-preview/start-390.png`.

## Обновление: три горизонтальные галереи

По новому запросу плоская сетка Sketchfab заменена конечной 3D-галереей из первых шести моделей. Все три раздела используют один контроллер и одинаковую механику. Изменены сглаживание (150 мс), непрерывные кривые глубины/поворота; замеры геометрии выполняются до записи стилей. Точки используют плавный переход с учётом reduced motion.

Проверка `.local-preview/carousel-check.py`: 66/66 passed (Edge, 1440 × 1000 и touch-эмуляция 390 × 844). Проверены количество карточек, прямой/обратный проход, покадровая монотонность сглаженного движения, выход и обратный вход во все три секции, свайпы в обе стороны, ссылки Sketchfab, отсутствие iframe, одновременное видео, возврат scrollY после мини-галереи и отсутствие JS-ошибок/горизонтального переполнения. Дополнительно пройдены reduced-motion и переходы меню/точек в разделе 3D.

Первый тест возврата использовал Playwright locator.click, который сам прокручивал страницу к ещё движущейся карточке перед нажатием. Диагностика показала: мини-галерея сохраняет и возвращает именно позицию в момент открытия. Повторная проверка использует обычное координатное нажатие мышью без служебной автопрокрутки; desktop/mobile проходят.

Снимки `.local-preview/models-carousel-False.png` и `.local-preview/models-carousel-True.png`; результаты `.local-preview/carousel-results.json`. Реальные мобильные устройства и субъективная плавность на другом оборудовании не проверены. Предыдущие результаты сетки 3 × 2 относятся к прежней версии, заменённой по запросу пользователя.
